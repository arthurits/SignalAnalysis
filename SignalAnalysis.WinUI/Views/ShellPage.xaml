<Page
    x:Class="SignalAnalysis.Views.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:SignalAnalysis.Helpers"
    xmlns:behaviors="using:SignalAnalysis.Behaviors"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:viewmodel="using:SignalAnalysis.ViewModels"
    Loaded="OnLoaded"
    d:DataContext="{d:DesignInstance Type=viewmodel:ShellViewModel, IsDesignTimeCreatable=False}"
    mc:Ignorable="d">

    <Grid RowDefinitions="Auto, *">
        <TitleBar
            x:Name="AppTitleBar"
            Title="{Binding MainWindowService.Title, Mode=TwoWay}"
            Height="{Binding ElementName=NavigationViewControl, Path=CompactPaneLength}"
            IsHitTestVisible="True"
            VerticalAlignment="Top"
            IsBackButtonVisible="True"
            IsBackButtonEnabled="{x:Bind ViewModel.IsBackEnabled, Mode=OneWay}"
            BackRequested="TitleBar_BackRequested"
            IsPaneToggleButtonVisible="True"
            PaneToggleRequested="TitleBar_PaneToggleRequested">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/AppLogo - V2.ico" />
            </TitleBar.IconSource>
        </TitleBar>

        <NavigationView
            x:Name="NavigationViewControl"
            Grid.Row="1"
            IsBackButtonVisible="Collapsed"
            IsBackEnabled="{x:Bind ViewModel.IsBackEnabled, Mode=OneWay}"
            IsPaneToggleButtonVisible="False"
            SelectedItem="{x:Bind ViewModel.Selected, Mode=OneWay}"
            IsSettingsVisible="False"
            ExpandedModeThresholdWidth="1280"
            OpenPaneLength ="220"
            DisplayModeChanged="NavigationViewControl_DisplayModeChanged"
            Header="{x:Bind ((ContentControl)ViewModel.Selected).Content, Mode=OneWay}">
            <NavigationView.Resources>
                <x:Double x:Key="NavigationViewItemOnLeftIconBoxHeight">24</x:Double> <!--https://stackoverflow.com/questions/76329586/how-can-i-change-the-height-of-the-navigationview-header-->
            </NavigationView.Resources>
            <NavigationView.MenuItems>
                <!--
                TODO: Update item titles by updating <x:Uid>.Content entries in Strings/en-us/Resources.resw.
                https://docs.microsoft.com/windows/uwp/app-resources/localize-strings-ui-manifest#refer-to-a-string-resource-identifier-from-xaml
            
                TODO: Update item icons by updating FontIcon.Glyph properties.
                https://docs.microsoft.com/windows/apps/design/style/segoe-fluent-icons-font#icon-list
                -->
                <NavigationViewItem Content="{Binding StrLibertyItem}"
                                    helpers:NavigationHelper.NavigateTo="SignalAnalysis.ViewModels.StartUpViewModel"
                                    ToolTipService.ToolTip="{Binding StrLibertyToolTip}">
                    <NavigationViewItem.Icon>
                        <ImageIcon Source="/Assets/Shell_StartUp.svg"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            <NavigationView.FooterMenuItems>
                <NavigationViewItemSeparator />
                <NavigationViewItem Content="{Binding StrSettingsItem}"
                                    helpers:NavigationHelper.NavigateTo="SignalAnalysis.ViewModels.SettingsViewModel"
                                    ToolTipService.ToolTip="{Binding StrSettingsToolTip}">
                    <NavigationViewItem.Icon>
                        <ImageIcon Source="/Assets/Settings_windows.svg"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="{Binding StrAboutItem}"
                                    helpers:NavigationHelper.NavigateTo="SignalAnalysis.ViewModels.AboutViewModel"
                                    ToolTipService.ToolTip="{Binding StrAboutToolTip}">
                    <NavigationViewItem.Icon>
                        <BitmapIcon UriSource="/Assets/about.ico"
                                    ShowAsMonochrome="False" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>
            <NavigationView.HeaderTemplate>
                <DataTemplate>
                    <Grid>
                        <TextBlock
                            Text="{Binding}"
                            Style="{ThemeResource TitleTextBlockStyle}" />
                    </Grid>
                </DataTemplate>
            </NavigationView.HeaderTemplate>
            <i:Interaction.Behaviors>
                <behaviors:NavigationViewHeaderBehavior
                    DefaultHeader="{x:Bind ((ContentControl)ViewModel.Selected).Content, Mode=OneWay}">
                    <behaviors:NavigationViewHeaderBehavior.DefaultHeaderTemplate>
                        <DataTemplate>
                            <Grid>
                                <TextBlock
                                    Text="{Binding}"
                                    Style="{ThemeResource TitleTextBlockStyle}" />
                            </Grid>
                        </DataTemplate>
                    </behaviors:NavigationViewHeaderBehavior.DefaultHeaderTemplate>
                </behaviors:NavigationViewHeaderBehavior>
            </i:Interaction.Behaviors>
            <Grid Margin="{StaticResource NavigationViewPageContentMargin}">
                <Frame x:Name="NavigationFrame" />
            </Grid>
        </NavigationView>
    </Grid>
</Page>
